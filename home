<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MindConnect - Your Mental Health Support Partner</title>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=Playfair+Display:wght@700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --green: #10a37f;
            --green-dark: #0c8a6b;
            --green-light: #e8f7f3;
            --ink: #0f1923;
            --ink2: #374151;
            --muted: #6b7280;
            --border: #e5e7eb;
            --surface: #f9fafb;
            --white: #ffffff;
        }
 
        * { margin: 0; padding: 0; box-sizing: border-box; }
 
        body {
            font-family: 'DM Sans', sans-serif;
            background: var(--white);
            color: var(--ink);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }
 
        /* NAV */
        header {
            position: fixed;
            top: 0; width: 100%;
            background: rgba(255,255,255,0.97);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--border);
            z-index: 999;
        }
 
        nav {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 68px;
        }
 
        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 22px;
            font-weight: 700;
            color: var(--green);
            text-decoration: none;
            letter-spacing: -0.02em;
        }
 
        .nav-links {
            display: flex;
            list-style: none;
            gap: 8px;
            align-items: center;
        }
 
        .nav-links a {
            text-decoration: none;
            color: var(--ink2);
            font-weight: 500;
            font-size: 15px;
            padding: 8px 14px;
            border-radius: 8px;
            transition: all 0.2s;
        }
 
        .nav-links a:hover { background: var(--green-light); color: var(--green); }
        .nav-links a.active { color: var(--green); font-weight: 600; }
 
        .nav-cta {
            background: var(--green) !important;
            color: white !important;
            padding: 9px 18px !important;
            border-radius: 8px !important;
        }
        .nav-cta:hover { background: var(--green-dark) !important; color: white !important; }
 
        /* HERO */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding-top: 68px;
            background: linear-gradient(160deg, #f0fdf8 0%, #ffffff 50%, #f5f3ff 100%);
            position: relative;
            overflow: hidden;
        }
 
        .hero::before {
            content: '';
            position: absolute;
            width: 600px; height: 600px;
            background: radial-gradient(circle, rgba(16,163,127,0.08) 0%, transparent 70%);
            top: -100px; right: -100px;
            border-radius: 50%;
        }
 
        .hero::after {
            content: '';
            position: absolute;
            width: 400px; height: 400px;
            background: radial-gradient(circle, rgba(99,102,241,0.06) 0%, transparent 70%);
            bottom: 50px; left: -50px;
            border-radius: 50%;
        }
 
        .hero-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: center;
        }
 
        .hero-left { position: relative; z-index: 1; }
 
        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 6px 14px;
            background: var(--green-light);
            border: 1px solid #b8ead8;
            border-radius: 50px;
            font-size: 13px;
            font-weight: 600;
            color: var(--green);
            margin-bottom: 28px;
            animation: fadeUp 0.6s ease both;
        }
 
        .hero-badge::before {
            content: '●';
            font-size: 8px;
            animation: pulse 2s infinite;
        }
 
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.4; }
        }
 
        .hero h1 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(44px, 5vw, 64px);
            font-weight: 800;
            line-height: 1.08;
            letter-spacing: -0.03em;
            margin-bottom: 24px;
            animation: fadeUp 0.6s ease 0.1s both;
        }
 
        .hero h1 .accent { color: var(--green); }
 
        .hero p {
            font-size: 18px;
            color: var(--muted);
            line-height: 1.65;
            margin-bottom: 40px;
            max-width: 480px;
            animation: fadeUp 0.6s ease 0.2s both;
        }
 
        .hero-buttons {
            display: flex;
            gap: 14px;
            flex-wrap: wrap;
            animation: fadeUp 0.6s ease 0.3s both;
        }
 
        .btn-primary {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 14px 28px;
            background: var(--green);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 15px;
            font-weight: 600;
            font-family: inherit;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s;
        }
 
        .btn-primary:hover {
            background: var(--green-dark);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(16,163,127,0.3);
        }
 
        .btn-secondary {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 14px 28px;
            background: white;
            color: var(--ink);
            border: 1.5px solid var(--border);
            border-radius: 10px;
            font-size: 15px;
            font-weight: 600;
            font-family: inherit;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s;
        }
 
        .btn-secondary:hover {
            border-color: var(--ink);
            transform: translateY(-2px);
        }
 
        /* Hero visual panel */
        .hero-right {
            position: relative;
            z-index: 1;
            animation: fadeUp 0.8s ease 0.2s both;
        }
 
        .hero-card-stack {
            position: relative;
        }
 
        .hero-card {
            background: white;
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 28px;
            box-shadow: 0 8px 40px rgba(0,0,0,0.08);
        }
 
        .hero-card-title {
            font-weight: 700;
            font-size: 15px;
            color: var(--ink);
            margin-bottom: 18px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
 
        .hero-card-title span {
            width: 28px; height: 28px;
            background: var(--green-light);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
        }
 
        .stat-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 14px;
            margin-bottom: 18px;
        }
 
        .mini-stat {
            background: var(--surface);
            border-radius: 10px;
            padding: 16px;
        }
 
        .mini-stat-num {
            font-family: 'Playfair Display', serif;
            font-size: 28px;
            font-weight: 700;
            color: var(--green);
            line-height: 1;
            margin-bottom: 4px;
        }
 
        .mini-stat-label {
            font-size: 12px;
            color: var(--muted);
            line-height: 1.4;
        }
 
        .support-badges {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }
 
        .badge {
            padding: 5px 12px;
            border-radius: 50px;
            font-size: 12px;
            font-weight: 600;
        }
 
        .badge-green { background: var(--green-light); color: var(--green); }
        .badge-indigo { background: #eef2ff; color: #6366f1; }
        .badge-amber { background: #fef3c7; color: #d97706; }
 
        .floating-pill {
            position: absolute;
            background: white;
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 12px 18px;
            font-size: 13px;
            font-weight: 600;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            display: flex;
            align-items: center;
            gap: 8px;
        }
 
        .pill-1 { top: -20px; right: -20px; }
        .pill-2 { bottom: -20px; left: -20px; }
 
        /* TRUST STRIP */
        .trust-strip {
            background: var(--surface);
            border-top: 1px solid var(--border);
            border-bottom: 1px solid var(--border);
            padding: 28px 0;
        }
 
        .trust-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
            display: flex;
            justify-content: space-around;
            align-items: center;
            flex-wrap: wrap;
            gap: 24px;
        }
 
        .trust-item {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 14px;
            color: var(--muted);
            font-weight: 500;
        }
 
        .trust-icon {
            width: 36px; height: 36px;
            background: white;
            border: 1px solid var(--border);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
        }
 
        /* SERVICES PREVIEW */
        .section {
            padding: 100px 0;
        }
 
        .section-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
        }
 
        .section-tag {
            display: inline-block;
            padding: 4px 12px;
            background: var(--green);
            color: white;
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            border-radius: 4px;
            margin-bottom: 16px;
        }
 
        .section-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(32px, 4vw, 48px);
            font-weight: 700;
            line-height: 1.15;
            letter-spacing: -0.02em;
            margin-bottom: 16px;
        }
 
        .section-sub {
            font-size: 17px;
            color: var(--muted);
            line-height: 1.65;
            max-width: 560px;
        }
 
        .section-header {
            margin-bottom: 56px;
        }
 
        .services-preview-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
        }
 
        .service-tile {
            background: white;
            border: 1px solid var(--border);
            border-radius: 14px;
            padding: 28px 24px;
            transition: all 0.25s;
            cursor: pointer;
            text-decoration: none;
            color: inherit;
            display: block;
        }
 
        .service-tile:hover {
            border-color: var(--green);
            transform: translateY(-4px);
            box-shadow: 0 12px 40px rgba(16,163,127,0.1);
        }
 
        .tile-icon {
            font-size: 28px;
            margin-bottom: 16px;
        }
 
        .tile-title {
            font-weight: 700;
            font-size: 16px;
            margin-bottom: 10px;
            color: var(--ink);
        }
 
        .tile-desc {
            font-size: 14px;
            color: var(--muted);
            line-height: 1.55;
        }
 
        /* MISSION STRIP */
        .mission-strip {
            background: var(--ink);
            padding: 80px 0;
        }
 
        .mission-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 2px;
        }
 
        .mission-stat {
            padding: 40px;
            text-align: center;
            border-right: 1px solid rgba(255,255,255,0.08);
        }
 
        .mission-stat:last-child { border-right: none; }
 
        .mission-num {
            font-family: 'Playfair Display', serif;
            font-size: 52px;
            font-weight: 700;
            color: var(--green);
            line-height: 1;
            margin-bottom: 12px;
        }
 
        .mission-label {
            font-size: 15px;
            color: rgba(255,255,255,0.6);
            line-height: 1.5;
        }
 
        /* ABOUT PREVIEW */
        .about-preview {
            background: var(--surface);
        }
 
        .about-preview-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 100px 24px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: center;
        }
 
        .about-text h2 {
            font-family: 'Playfair Display', serif;
            font-size: 42px;
            font-weight: 700;
            line-height: 1.2;
            letter-spacing: -0.02em;
            margin-bottom: 20px;
        }
 
        .about-text p {
            font-size: 16px;
            color: var(--muted);
            line-height: 1.7;
            margin-bottom: 14px;
        }
 
        .about-visual {
            background: white;
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 36px;
            box-shadow: 0 8px 40px rgba(0,0,0,0.06);
        }
 
        .about-visual-title {
            font-weight: 700;
            font-size: 16px;
            margin-bottom: 24px;
        }
 
        .value-item {
            display: flex;
            align-items: flex-start;
            gap: 14px;
            margin-bottom: 20px;
        }
 
        .value-item:last-child { margin-bottom: 0; }
 
        .value-dot {
            width: 32px; height: 32px;
            background: var(--green-light);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            flex-shrink: 0;
            margin-top: 2px;
        }
 
        .value-item h4 { font-weight: 600; font-size: 15px; margin-bottom: 4px; }
        .value-item p { font-size: 13px; color: var(--muted); line-height: 1.5; margin: 0; }
 
        /* CTA SECTION */
        .cta-section {
            padding: 100px 0;
            text-align: center;
        }
 
        .cta-inner {
            max-width: 600px;
            margin: 0 auto;
            padding: 0 24px;
        }
 
        .cta-section h2 {
            font-family: 'Playfair Display', serif;
            font-size: 42px;
            font-weight: 700;
            margin-bottom: 16px;
            letter-spacing: -0.02em;
        }
 
        .cta-section p {
            font-size: 17px;
            color: var(--muted);
            margin-bottom: 36px;
            line-height: 1.6;
        }
 
        /* FOOTER */
        footer {
            background: var(--ink);
            color: rgba(255,255,255,0.5);
            padding: 48px 0;
        }
 
        .footer-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }
 
        .footer-logo {
            font-family: 'Playfair Display', serif;
            font-size: 20px;
            font-weight: 700;
            color: white;
        }
 
        .footer-links {
            display: flex;
            gap: 24px;
        }
 
        .footer-links a {
            text-decoration: none;
            color: rgba(255,255,255,0.5);
            font-size: 14px;
            transition: color 0.2s;
        }
 
        .footer-links a:hover { color: white; }
 
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
 
        /* Scroll animation */
        .reveal {
            opacity: 0;
            transform: translateY(24px);
            transition: all 0.7s ease;
        }
 
        .reveal.in-view {
            opacity: 1;
            transform: translateY(0);
        }
 
        /* Mobile */
        @media (max-width: 900px) {
            .hero-inner { grid-template-columns: 1fr; gap: 48px; }
            .hero-right { display: none; }
            .services-preview-grid { grid-template-columns: repeat(2, 1fr); }
            .mission-inner { grid-template-columns: 1fr; }
            .mission-stat { border-right: none; border-bottom: 1px solid rgba(255,255,255,0.08); padding: 30px; }
            .about-preview-inner { grid-template-columns: 1fr; gap: 40px; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>
 
<header>
    <nav>
        <a href="index.html" class="logo">MindConnect</a>
        <ul class="nav-links">
            <li><a href="services.html">Services</a></li>
            <li><a href="network.html">Network</a></li>
            <li><a href="mission.html">Mission</a></li>
            <li><a href="about.html">About</a></li>
            <li><a href="contact.html" class="nav-cta">Get Started</a></li>
        </ul>
    </nav>
</header>
 
<!-- HERO -->
<section class="hero">
    <div class="hero-inner">
        <div class="hero-left">
            <div class="hero-badge">Trusted by 100+ patients</div>
            <h1>Your Mental Health <span class="accent">Support Partner</span></h1>
            <p>Transforming care through personalized treatment plans, expert medical guidance, and complete support for lasting health outcomes.</p>
            <div class="hero-buttons">
                <a href="contact.html" class="btn-primary">Get Started Today →</a>
                <a href="about.html" class="btn-secondary">Learn Our Story</a>
            </div>
        </div>
        <div class="hero-right">
            <div class="hero-card-stack">
                <div class="floating-pill pill-1">🟢 24/7 Support Available</div>
                <div class="hero-card">
                    <div class="hero-card-title">
                        <span>📊</span> At a Glance
                    </div>
                    <div class="stat-row">
                        <div class="mini-stat">
                            <div class="mini-stat-num">60M</div>
                            <div class="mini-stat-label">Americans affected by mental illness</div>
                        </div>
                        <div class="mini-stat">
                            <div class="mini-stat-num">500K+</div>
                            <div class="mini-stat-label">People in our region</div>
                        </div>
                    </div>
                    <div class="support-badges">
                        <span class="badge badge-green">Nutrition Planning</span>
                        <span class="badge badge-indigo">Medication Mgmt</span>
                        <span class="badge badge-amber">24/7 Support</span>
                        <span class="badge badge-green">Health Education</span>
                    </div>
                </div>
                <div class="floating-pill pill-2">🌎 English & Spanish</div>
            </div>
        </div>
    </div>
</section>
 
<!-- TRUST STRIP -->
<div class="trust-strip">
    <div class="trust-inner">
        <div class="trust-item">
            <div class="trust-icon">🏥</div> Medical Center Partnerships
        </div>
        <div class="trust-item">
            <div class="trust-icon">👨‍⚕️</div> Board-Certified Specialists
        </div>
        <div class="trust-item">
            <div class="trust-icon">🌿</div> Evidence-Based Care
        </div>
        <div class="trust-item">
            <div class="trust-icon">🌎</div> English & Spanish
        </div>
        <div class="trust-item">
            <div class="trust-icon">🔒</div> HIPAA Compliant
        </div>
    </div>
</div>
 
<!-- SERVICES PREVIEW -->
<section class="section">
    <div class="section-inner">
        <div class="section-header reveal">
            <div class="section-tag">Our Services</div>
            <h2 class="section-title">Comprehensive Care</h2>
            <p class="section-sub">Everything you need to manage your mental health — delivered through our expert-designed support system.</p>
        </div>
        <div class="services-preview-grid reveal">
            <a href="services.html" class="service-tile">
                <div class="tile-icon">🥗</div>
                <div class="tile-title">Nutrition Planning</div>
                <div class="tile-desc">Personalized meal plans crafted by certified nutritionists to optimize your wellbeing.</div>
            </a>
            <a href="services.html" class="service-tile">
                <div class="tile-icon">💊</div>
                <div class="tile-title">Medication Management</div>
                <div class="tile-desc">Secure medication storage with smart dispensing technology for vulnerable populations.</div>
            </a>
            <a href="services.html" class="service-tile">
                <div class="tile-icon">📚</div>
                <div class="tile-title">Health Education</div>
                <div class="tile-desc">Comprehensive mental health programs covering lifestyle, prevention, and optimization.</div>
            </a>
            <a href="services.html" class="service-tile">
                <div class="tile-icon">🛟</div>
                <div class="tile-title">24/7 Support</div>
                <div class="tile-desc">Round-the-clock access to certified professionals and peer support specialists.</div>
            </a>
        </div>
    </div>
</section>
 
<!-- MISSION NUMBERS -->
<div class="mission-strip">
    <div class="mission-inner">
        <div class="mission-stat reveal">
            <div class="mission-num">60M</div>
            <div class="mission-label">Americans affected by mental illness per the NIH</div>
        </div>
        <div class="mission-stat reveal">
            <div class="mission-num">500K+</div>
            <div class="mission-label">People affected in our region alone</div>
        </div>
        <div class="mission-stat reveal">
            <div class="mission-num">35</div>
            <div class="mission-label">PCPs per 100k residents in underserved areas</div>
        </div>
    </div>
</div>
 
<!-- ABOUT PREVIEW -->
<div class="about-preview">
    <div class="about-preview-inner">
        <div class="about-text reveal">
            <div class="section-tag">About Us</div>
            <h2>Empowering Communities from the Inland Empire</h2>
            <p>MindConnect was founded to reduce health disparities in underserved communities — empowering individuals through education, resources, and community support.</p>
            <p>Through partnerships with certified mental health experts, therapists, and healthcare professionals, we deliver evidence-based care combined with personalized support.</p>
            <a href="about.html" class="btn-secondary" style="margin-top:12px; display: inline-flex;">Learn More →</a>
        </div>
        <div class="about-visual reveal">
            <div class="about-visual-title">Our Core Values</div>
            <div class="value-item">
                <div class="value-dot">🤝</div>
                <div>
                    <h4>Community First</h4>
                    <p>We meet you where you are — in English or Spanish — with no judgment and full support.</p>
                </div>
            </div>
            <div class="value-item">
                <div class="value-dot">🔬</div>
                <div>
                    <h4>Evidence-Based</h4>
                    <p>Every program and recommendation is grounded in clinical research and expert guidance.</p>
                </div>
            </div>
            <div class="value-item">
                <div class="value-dot">🌱</div>
                <div>
                    <h4>Lasting Change</h4>
                    <p>We focus on long-term outcomes, not quick fixes, building lasting health habits.</p>
                </div>
            </div>
        </div>
    </div>
</div>
 
<!-- CTA -->
<section class="cta-section">
    <div class="cta-inner">
        <h2 class="reveal">Ready to Begin Your Journey?</h2>
        <p class="reveal">No one should face mental health challenges alone. Connect with our team today and take your first step toward lasting wellbeing.</p>
        <div class="reveal">
            <a href="contact.html" class="btn-primary" style="font-size:16px; padding:16px 36px;">Start Today — It's Free →</a>
        </div>
    </div>
</section>
 
<footer>
    <div class="footer-inner">
        <div class="footer-logo">MindConnect</div>
        <div class="footer-links">
            <a href="services.html">Services</a>
            <a href="network.html">Network</a>
            <a href="mission.html">Mission</a>
            <a href="about.html">About</a>
            <a href="contact.html">Contact</a>
        </div>
        <div style="font-size:13px;">© 2025 MindConnect. Empowering communities.</div>
    </div>
</footer>
 
<script>
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('in-view'); });
    }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });
    document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>
</body>
</html>
 
